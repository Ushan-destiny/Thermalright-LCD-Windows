import controller
if __name__ == "__main__":
    # This runs the main function from your controller.py silently
    # We pass None as config path so it finds it automatically
    controller.main(None)